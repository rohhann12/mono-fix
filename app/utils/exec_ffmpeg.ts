import { exec } from "child_process";
import path from "path";

export async function convertToStereo(inputPath: string, outputPath: string): Promise<void> {
  return new Promise((resolve, reject) => {
    // download video 
    // feed them to ffmpeg
    // reuplload
  })
}
